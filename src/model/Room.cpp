#include "Room.h"

// Room is a plain-data struct; all member functions are defined inline
// in Room.h. This translation unit exists so that CMake and the linker
// have a consistent object file for the type if needed in the future.

namespace Cluedo {
} // namespace Cluedo
